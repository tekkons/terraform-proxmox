# terraform-proxmox

To provision this project, run the following from within working directory

- `terraform init` to get the modules
- `terraform plan` to see the infrastructure plan
- `terraform apply` to create the infrastructure build
- `terraform destroy` to destroy the built infrastructure
